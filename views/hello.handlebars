{{#each  filteredBlogs as |blog|}}
<div class="card">
  <h5 class="card-header">Blog Id: {{{blog.blog_id}}}</h5>
  <div class="card-body">
    <h5 class="card-title"> {{{blog.created_at}}} {{{blog.blog_title}}} </h5>
    <p class="card-text">{{{blog.blog_post}}}</p>
  </div>
</div>
{{/each}}
